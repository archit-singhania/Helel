# Model contracts

Versioned local model input, output, tokenizer, and checkpoint contracts belong here. Phase 6 defines dataset-manifest and byte-BPE tokenizer schemas; later phases add tensor, checkpoint, and generation contracts.
