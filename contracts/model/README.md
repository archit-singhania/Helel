# Model contracts

Versioned local model input, output, tokenizer, and checkpoint contracts belong here. The current contracts cover dataset manifests, byte-BPE tokenizers, model configurations, checksummed checkpoints, bounded generation requests, and constrained agent proposals.
