# Model contracts

Versioned local model input, output, tokenizer, and checkpoint contracts belong here. Phase 6 defines dataset-manifest and byte-BPE tokenizer schemas. Phase 7 adds model-configuration and checksummed-checkpoint schemas; generation request contracts arrive with desktop inference.
