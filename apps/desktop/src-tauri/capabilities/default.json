{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability with no privileged plugins",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:allow-open"]
}
